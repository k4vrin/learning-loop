[project]
name = "learning-loop"
version = "0.4.1"
description = "Deterministic Markdown workspace state for the Learning Loop Agent Plugin"
requires-python = ">=3.11"
license = "Apache-2.0"

[tool.pyright]
pythonVersion = "3.11"
